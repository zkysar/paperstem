// User-visible em-dash that represents "the previous section ends here,
// no next song named yet." Used by the Shift+M shortcut and by the
// popover's "End here" action. A one-character label is intentional —
// the user can always rename it later via the existing Label mode if
// the gap actually represents something nameable.
export const END_SECTION_LABEL = '—';
